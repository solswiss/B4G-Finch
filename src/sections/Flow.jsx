import Section from "../components/Section"

const Flow = () => {
    return (
        <Section id="flow">
            <div className="container relative">
                Flow
            </div>
        </Section>
    );
};

export default Flow;